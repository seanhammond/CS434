Requirements for running:
-Java Version 1.8(jre_1.8.0_40)

To Run:
-Execute the following command on windows:
	java -jar assignment3.jar

-Alternatively you can load the source code into a Java IDE such as eclipse and run there

Source code and inputs:
-Source code is found in the directory src/
-Input files for KNN and DT are also contained in the directory src/
